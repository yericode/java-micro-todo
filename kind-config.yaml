# kind create cluster --config kind-config.yaml
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30078
        hostPort: 30078
        protocol: TCP
      - containerPort: 30079
        hostPort: 30079
        protocol: TCP