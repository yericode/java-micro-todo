server:
  port: 8078
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/DB_USER
    username: root
    password: password
  mvc:
    problemdetails:
      enabled: true
services:
  todo-service:
    uri: localhost:8079  