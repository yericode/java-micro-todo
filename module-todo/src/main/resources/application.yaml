server:
  port: 8079
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/DB_TODO
    username: root
    password: password
  mvc:
    problemdetails:
      enabled: true
logging:
  level:
    root: debug
services:
  user-service:
    uri: http://localhost:8078
  todo-service:
    uri: http://localhost:8079